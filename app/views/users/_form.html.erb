<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :Name, id: :user_name, class:"w3-input w3-light-grey"%>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :Email, id: :user_email, class:"w3-input light-grey"%>
  </div>

  <div class="actions">
    <%= form.submit :Submit, class:"w3-button w3-light-grey"%>
  </div>


<% end %>
